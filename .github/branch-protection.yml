# Declarative branch-protection spec for `main`.
#
# Consumed by `probot/settings` (https://github.com/apps/settings) or
# `repository-settings-syncer`. To sync manually:
#   gh api -X PUT /repos/:owner/:repo/branches/main/protection \
#     --input .github/branch-protection-payload.json
# (Generate payload.json by converting this yaml — a helper script will land
#  alongside the settings bot when it is installed.)
#
# Required checks are intentionally limited to the surviving Python/data CI jobs.
# Frontend/deployment and temporary phase-gate checks were removed when the
# repository became server-simulation first.

branches:
  - name: main
    protection:
      required_pull_request_reviews:
        required_approving_review_count: 1
        dismiss_stale_reviews: true
        require_code_owner_reviews: false
      required_status_checks:
        strict: true
        contexts:
          - ci / lint
          - ci / type
          - ci / test
          - ci / schema-drift
          - ci / schema-compat
      enforce_admins: true
      required_linear_history: true
      allow_force_pushes: false
      allow_deletions: false
      required_conversation_resolution: true
      restrictions: null
